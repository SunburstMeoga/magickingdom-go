# 开发环境配置
server:
  port: 8080
  mode: debug # debug, release, test

database:
  host: localhost
  port: 3306
  user: root
  password: 1013
  dbname: magickingdom
  charset: utf8mb4
  parse_time: true
  loc: Local
  max_idle_conns: 10
  max_open_conns: 100

jwt:
  secret: your-secret-key-change-in-production
  expire_hours: 168 # 7天

wechat:
  app_id: wx9cc8e087577e00d0
  app_secret: 7d1b159aabe9af4aef15b733a60bdf53
  login_url: https://api.weixin.qq.com/sns/jscode2session

log:
  level: debug # debug, info, warn, error
  format: json # json, text
  output: stdout # stdout, file
  file_path: logs/app.log
